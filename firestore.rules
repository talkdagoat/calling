rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Registered Talk Users
    match /users/{userId} {
      allow read, write: if true;
    }

    // Saved Contacts
    match /contacts/{contactId} {
      allow read, write: if true;
    }
  }
}
